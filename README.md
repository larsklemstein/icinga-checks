# Icinga checks

## check_docker_logs

Purpose: Assure that the container logs follow the defined limits
for amount and size. Assure that there are no docker containers with either
unconfigured or misconfigured log file settings.
